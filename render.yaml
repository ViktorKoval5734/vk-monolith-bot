services:
  - type: web
    name: vk-monolith-bot
    env: python
    plan: free
    buildCommand: pip install -r requirements.txt
    startCommand: python bot.py
    healthCheckPath: /
    envVars:
      - key: PORT
        value: 8000
      - key: PYTHON_VERSION
        value: 3.11